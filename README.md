# PlanRoute — deployed site

Encrypted build of PlanRoute (password-protected). Source lives in the private
repo `AlexMC/planroute-app`; this repo only ever contains the AES-256-encrypted
page that GitHub Pages serves. Deploys are pushed by `deploy.sh` in the source repo.
